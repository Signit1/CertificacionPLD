{
  "name": "html-renderer",
  "version": "1.0.0",
  "description": "Simple app to render HTML files with JavaScript",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build.js",
    "prestart": "npm run build",
    "predev": "npm run build",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
